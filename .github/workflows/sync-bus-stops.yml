name: Sync corrected bus stops

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/sync_bus_stops.py"
      - ".github/workflows/sync-bus-stops.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Generate corrected bus stop JSON
        run: python scripts/sync_bus_stops.py
      - name: Commit generated JSON
        run: |
          if git diff --quiet -- data/bus_stops_corrected.json; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/bus_stops_corrected.json
          git commit -m "Sync corrected bus stop coordinates"
          git push
